@model SIGAPPBOM.Servicio.ViewModels.PedidoViewModel
<div class="container">
    <div class="block">
        <div class="block_head">
            <div class="bheadl">
            </div>
            <div class="bheadr">
            </div>
            <h2>@ViewBag.Titulo
            </h2>
            <a class="pull-right linksInTitle" href="@Url.Action("MostrarPedidos", "PedidosInsumos")">
                << Pedidos</a>
        </div>
        <div class="block_content">
            <br />
            @Html.Partial("_Pedido")
            <div class="well" style="background-color: transparent; width: 91%; margin-left: 20px;">
                <a id="btnEliminar" class="btn btn-danger" href="@Url.Action("EliminarPost", new { pedidoId = Model.Id })">
                    <i class="icon-trash icon-white"></i>Eliminar</a>
            </div>
        </div>
    </div>
</div>
